import React from 'react';
import {Icon} from '@ui-kitten/components';

export const HeartIcon = (props) => <Icon {...props} name="heart" />;

export const PinIcon = (props) => <Icon {...props} name="pin" />;

export const AlertIcon = (props) => <Icon {...props} name="alert-circle-outline" />;

export const RefreshIcon = (props) => (
  <Icon {...props} name="refresh-outline" />
);

export const BackIcon = (props) => <Icon {...props} name="arrow-back" />;

export const SearchIcon = (props) => <Icon {...props} name="search-outline" />;
